using Avalonia.Controls;

namespace MapWizard.Theme.Controls;

/// <summary>
/// A navigation button whose chrome stays neutral so hover and selection can be
/// expressed by the navigation glyph instead of flashing the entire row.
/// </summary>
public sealed class NavigationButton : Button
{
}
