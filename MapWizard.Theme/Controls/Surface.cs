using Avalonia.Controls;

namespace MapWizard.Theme.Controls;

/// <summary>
/// A lightweight themed surface. It intentionally remains a Border so callers keep
/// normal Avalonia layout, clipping and child semantics.
/// </summary>
public sealed class Surface : Border
{
}
