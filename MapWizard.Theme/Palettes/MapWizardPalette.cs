namespace MapWizard.Theme.Palettes;

/// <summary>
/// Visual palettes known to <see cref="MapWizardPaletteCatalog"/>.
/// Kept in the Theme package so the catalog does not depend on application settings types.
/// </summary>
public enum MapWizardPalette
{
    Noir,
    Classic,
    Gruvbox
}
